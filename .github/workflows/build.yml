name: Wails build

# 仅在打 Tag 时触发构建
on:
  push:
    tags:
      - '*' # 匹配任何新 Tag

# 环境变量：增加内存限制，避免 OOM
env:
  NODE_OPTIONS: "--max-old-space-size=4096"

jobs:
  build:
    strategy:
      # 任何平台构建失败都不会影响其他平台的构建
      fail-fast: false
      matrix:
        build:
          - name: 'Linux'
            platform: 'linux/amd64'
            os: 'ubuntu-latest'
          - name: 'Windows'
            platform: 'windows/amd64'
            os: 'windows-latest'

    runs-on: ${{ matrix.build.os }}

    steps:
      - name: ⚙️ Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: 🔨 Build Wails App (${{ matrix.build.name }})
        # 【关键点】根据您的经验，使用 @main 确保获得最新的依赖修复
        uses: dAppServer/wails-build-action@main
        id: build
        with:
          # 使用一个统一的应用名，Wails 会自动添加平台后缀
          build-name: 'Go_Arknights_Gacha_App'
          build-platform: ${{ matrix.build.platform }}
          package: true
          go-version: '1.23'

      - name: 📦 Upload Build Artifact
        # 使用 v4 稳定版本上传 Artifact
        uses: actions/upload-artifact@v4
        with:
          # 使用 matrix.build.name 作为 Artifact 名称
          name: ${{ matrix.build.name }}-build
          path: build/bin/*
          retention-days: 1

  release:
    # 仅在 build 任务完成后执行
    needs: build
    # 仅在 Tag Push 触发时执行
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: ⬇️ Download All Build Artifacts
        # 使用 v4 稳定版本下载 Artifact
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 🚀 Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
            files: artifacts/**/*
            token: ${{ secrets.GITHUB_TOKEN }}
            body: |
              Automated Release for **${{ github.ref_name }}**
              
              ### 包含的平台
              - Linux (x64)
              - Windows (x64)